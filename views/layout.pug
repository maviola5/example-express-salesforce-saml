doctype html
html
  head
    title= title
    style(type="text/css").
      body {
        padding: 50px;
        font: 14px "Lucida Grande", Helvetica, Arial, sans-serif;
      }
      a {
        color: #00B7FF;
        padding-right: 1rem;
      }
  body
    block content